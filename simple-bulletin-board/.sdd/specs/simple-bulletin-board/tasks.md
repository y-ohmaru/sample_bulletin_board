# 実装タスクリスト

## セクション1：データモデル実装
- [x] 1.1 Post エンティティと PostForm を作成する
  - design.md で定義した `Post` の `id`、`name`、`content`、`createdAt` を実装する
  - `PostForm` に `name` と `content` を定義し、必須入力と100文字以内のバリデーションルールを実装する
- [x] 1.2 PostRepository とデータベース設定を実装する
  - H2 Database へ接続できる設定を追加する
  - 投稿の保存、一覧取得、削除を行う永続化層を実装する
  - アプリ起動時に `posts` テーブルを利用可能にする

## セクション2：ビジネスロジック実装
- [x] 2.1 PostService の一覧取得と登録処理を実装する
  - design.md の処理フロー 1、3、5 に対応する
  - 投稿一覧取得、Post エンティティ生成、保存処理を実装する
- [x] 2.2 PostService の削除処理を実装する
  - design.md の処理フロー 7 に対応する
  - 投稿 ID を受け取って削除する処理と、存在しない投稿への安全な対応を実装する
- [x] 2.3 エラーハンドリング方針を実装する
  - design.md のエラーハンドリングに対応する
  - バリデーション失敗時の再表示、削除対象未存在時の扱い、起動時の DB 利用可能状態を確認する

## セクション3：インターフェース実装
- [x] 3.1 PostController の一覧表示エンドポイントを作成する
  - `GET /posts` を実装し、投稿一覧と空の PostForm を `posts/list.html` に渡す
  - design.md の処理フロー 1、2 に対応する
- [x] 3.2 PostController の登録・削除エンドポイントを作成する
  - 投稿登録の POST と投稿削除の POST を実装する
  - 成功時は `GET /posts` へリダイレクトする
  - design.md の処理フロー 3、5、6、7、8 に対応する
- [x] 3.3 `posts/list.html` を実装する
  - 投稿一覧、投稿フォーム、削除操作を1画面にまとめる
  - 入力値の再表示とエラーメッセージ出力を実装する

## セクション4：統合とテスト
- [x] 4.1 コンポーネントを統合する
  - Controller、Service、Repository、Entity、Form、Template を接続する
  - 一覧表示、登録、削除のデータフローが通ることを確認する
- [x] 4.2 基本的な動作テストを実装する
  - 投稿登録、一覧表示、削除、バリデーションを対象にユニットテストまたは統合テストを追加する
  - H2 を用いた基本的な動作確認ができる状態にする
- [ ] 4.3 要件の受入基準を確認する
  - requirements.md の受入基準に沿って一覧表示、登録、削除、入力チェック、永続化を確認する
  - 未達成項目があれば修正対象として明確化する

## 実装メモ
- `src/test/java/com/example/bulletinboard/` 配下にフォーム、サービス、コントローラー、統合テストを追加済み
- この作業環境では `java` と `mvn` が未導入のため、テスト実行・Lint 実行・受入基準の実機確認は未完了
- 次のアクション：JDK 21 と Maven を導入し、`mvn test` を実行して 4.3 を完了させる
